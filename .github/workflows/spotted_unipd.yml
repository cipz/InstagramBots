name: Astronomy picture of the day
on: 
  # To manually execute action
  [workflow_dispatch]
#  schedule:
#    # * is a special character in YAML so you have to quote this string
#     - cron:  '30 */10 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        ref: spotted_unipd
    - name: Set up Python 3.8
      uses: actions/setup-python@v2
      with:
        python-version: 3.8
    - name: Installing latex
      run: |
        sudo apt-get install texlive-full -y
        sudo apt install poppler-utils
    - name: Installing dependencies
      working-directory: "spotted_unipd"
      run: |
        pip3 install instabot bs4 wget pdf2image poppler-utils
    - name: Running python script
      env:
        spotted_username: ${{ secrets.spotted_username }}
        spotted_password: ${{ secrets.spotted_password }}
      working-directory: "astronomy_picture_of_the_day"
      run: |
        python3 spotted_unipd.py
    - name: Commit files if any
      id: commit
      working-directory: "spotted_unipd"
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "github-actions"
        git add params.json
        if [-z "$(git status --porcelain)"]; then
          echo "::set-output name=push::false"
        else
          git commit -m "New post from spotted_unipd" -a
          echo "::set-output name=push::true"
        fi
      continue-on-error: true
      shell: bash
    - name: Push changes if any
      if: steps.commit.outputs.push == 'true'
      uses: ad-m/github-push-action@spotted_unipd
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
      continue-on-error: true
